<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.wordlikeproject">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SearchingResultWindow"></activity>
        <activity android:name=".WordRetouchWindow" />
        <activity android:name=".WordAddWindow" />
        <activity android:name=".WordsListsDownloadCenter" />
        <activity android:name=".WordsListWindow" />
        <activity android:name=".WordBookListWindow" />
        <activity android:name=".ExamWindow" />
        <activity android:name=".NotificationWindow" />
        <activity android:name=".MainWindow" />
        <activity android:name=".SignWindow">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>