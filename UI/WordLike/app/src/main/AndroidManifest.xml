<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.wordlike">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".WordListsDownloadItem"></activity>
        <activity android:name=".WordsListItem" />
        <activity android:name=".WordBookItemActivity" />
        <activity android:name=".PopUpWordsListWindow" />
        <activity android:name=".AddWordWindow" />
        <activity android:name=".FixWordWindow" />
        <activity android:name=".WordListsDownload" />
        <activity android:name=".Exam" />
        <activity android:name=".NotificationWindow" />
        <activity android:name=".WordsListWindow" />
        <activity android:name=".MainWindow" />
        <activity android:name=".SignWindow" />
        <activity android:name=".WordBookWindow">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>